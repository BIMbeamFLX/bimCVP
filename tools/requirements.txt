# Sovereign AEC — Python tools dependencies
# install via:   pip install -r requirements.txt

# ----- IFC parsing -----
ifcopenshell>=0.8.0       # IFC4.3 ADD2 schema support

# ----- Nostr -----
pynostr>=0.7.0            # client lib for events, relays, signing

# ----- HTTP / Blossom upload -----
requests>=2.32.0

# ----- OpenTimestamps (für Bautagebuch-Anker) -----
opentimestamps-client>=0.7.0

# ----- Optional: IFC validation -----
# ifcopenshell-tools comes with ifcopenshell, includes bSI validator hooks

# ----- Optional: Template-Engine für Gebäudebuch-Generator -----
jinja2>=3.1.0
python-docx>=1.1.0        # für DOCX-Export (Phase 2)
reportlab>=4.0.0          # für PDF-Render (verwendet auch im NIP-Fitness-PDF-Builder)

# ----- Optional: bSDD-Abfragen -----
# requests reicht für die REST-API, kein separates SDK nötig

# ----- Dev-Tools -----
ruff>=0.6.0               # Lint + Format
pytest>=8.0.0             # Tests
